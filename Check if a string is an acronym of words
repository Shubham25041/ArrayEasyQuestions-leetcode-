class Solution {
public:
    bool isAcronym(vector<string>& words, string s) {

        string ans="";
        for(auto word : words){
            ans.push_back(word.front());
        }

        return s==ans;
    }
};
